#include <stdio.h>
#include <string.h> 
int main(){
    char nome[50];  // Declara um array para armazenar o nome
    
    printf("Seu nome? ");
    scanf("%s", nome);  // Lê a string e armazena em 'nome'
    
    printf("Seu nome é %s\n", nome);  // Usa %s para imprimir string
        if (strcmp(nome, "Hiago") == 0){
    printf("Caguei");
        }
        else{
    printf("Gozei");
            
        }
    return 0;
}
